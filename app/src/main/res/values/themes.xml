<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PDV" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/dark_primary</item>
        <item name="colorOnPrimary">@color/light_grey</item>
        <item name="colorSurface">@color/light_primary</item>
        <item name="android:textColorPrimary">@color/light_grey</item>
        <item name="android:itemBackground">@color/light_primary</item>
        <item name="android:navigationBarColor">@color/dark_primary</item>
    </style>

    <style name="Theme.PDV" parent="Base.Theme.PDV" />
</resources>